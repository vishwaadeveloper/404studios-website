export { useContactForm } from './hooks/useContactForm';
export { 
  contactInfo, 
  availableTimeSlots, 
  projectTypes, 
  budgetRanges, 
  timelineOptions, 
  packageOptions 
} from './data/contactData';
export type { 
  ContactFormData, 
  TimeSlot, 
  ContactInfo, 
  FormStep 
} from './types/contact.types';
